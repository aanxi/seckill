<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.practice.seckill.api.mapper.OrderMapper">
    <select id="selectSeckillGoodsIds" resultType="List">
        select goods_id from seckill_act
        where status = 0 or status = 1
    </select>
</mapper>